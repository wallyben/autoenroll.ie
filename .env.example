# AutoEnroll.ie environment
JWT_SECRET=change-me
STRIPE_SECRET=
HASH_SALT=demo-salt
# Set CORS_ORIGIN to * for development, or specify allowed origins for production
CORS_ORIGIN=*
# NEXT_PUBLIC_API_URL can be:
# - /api (will use Next.js API proxy - recommended for Codespaces)
# - http://localhost:4000 (for local development)
# - Your actual API URL for production
NEXT_PUBLIC_API_URL=/api
